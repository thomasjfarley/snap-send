import { View, Text } from 'react-native';

export default function EditScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Step 2: Edit (Crop, Filter, Frame)</Text>
    </View>
  );
}
