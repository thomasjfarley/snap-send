import { View, Text } from 'react-native';

export default function PhotoScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Step 1: Choose Photo</Text>
    </View>
  );
}
